{% extends '::base.html.twig' %}
{% block body %}

    {{ form_start(form, {'action': path('itkg_consumer.service.update', {'id': id })}) }}
        {{ form_errors(form) }}

        {{ form_row(form) }}

    <input type="submit" />
    {{ form_end(form) }}

{% endblock %}
